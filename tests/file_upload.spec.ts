import { test, expect } from '@playwright/test';
import { FileUploadPage } from '../pages/FileUploadPage';

test.describe('File Upload', () => {
  // File upload tests removed due to server error
});
